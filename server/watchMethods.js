

Meteor.methods({
  
  getWatchlistData(type, keyword) {
    /*
    Meteor.users.update(Meteor.userId(), {
      $set: {
        unlockSpeed: setTime,
      }
    });
    */
    return true;
  },
  
  
});