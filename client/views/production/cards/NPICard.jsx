import React from 'react';

// import JumpText from '/client/components/tinyUi/JumpText.jsx';


const NPICard = ({ allGroup, allWidget, allVariant, user, app })=> {
  
  // const v = w.versions.sort((v1, v2)=> {
  //             if (v1.version < v2.version) { return 1 }
  //             if (v1.version > v2.version) { return -1 }
  //             return 0;
  //           });
  // <JumpText title={g.alias} link={g.alias} />
  
  return(
    <div className='space' key='NP1M0D3'>
        
      <hr />
      
        <em>NPI Time is under construction</em>
          
    </div>
  );
};

export default NPICard;